import 'package:flutter/material.dart';

class AiTutorScreen extends StatelessWidget {
  const AiTutorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AI Tutor Screen', style: TextStyle(fontSize: 20)),
    );
  }
}
